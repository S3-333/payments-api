# Copia este archivo a .env y ajusta valores. La app carga variables con python-dotenv.

# MySQL (mismo host/puerto que docker-compose si usas el contenedor)
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/payments

# true = imprime SQL en consola (solo desarrollo)
SQL_ECHO=false
